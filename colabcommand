!pip install https://github.com/yt-dlp/yt-dlp/archive/master.tar.gz
!pip install youtube-search-python

!git config --global init.defaultBranch main
!git init
!git remote add origin https://github.com/nguyenduc13475/super_model_ai.git
!git fetch origin
!git merge origin/main

!git fetch origin
!git merge origin/main

for i in range(2, 14):
            if type(self.forward_diffusion_layer[i]) == nn.Conv2d:
                latent_ = self.forward_diffusion_layer[i](latent)
            else:
                latent_, time_encoding = self.forward_diffusion_layer[i](latent_, time_encoding)
            S.append(latent_)